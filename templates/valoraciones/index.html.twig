{% extends 'base.html.twig' %}

{% block title %}WaterBoat
{% endblock %}

{% block body %}
		<div class="container">
                    {{ form_start(form) }}
                    <div class="form-group col-md-3">
                        {{ form_row(form.valoracion) }}
                    </div>
                    <div class="form-group col-md-3">
                        {{ form_row(form.comentario) }}
                    </div>
					<div class="form-group col-md-3">     
						<button type="submit" class="btn btn-primary py-3 px-5">Guardar Valoracion</button>
					</div>
            		{{ form_end(form) }}
				</div>
     	</div>

		
{% endblock %}

